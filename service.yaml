apiVersion: v1
kind: Service
metadata:
  name: mini-project-b-service
spec:
  type: NodePort
  selector:
    app: mini-project-b
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30000
