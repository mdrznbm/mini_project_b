# Python cache files
__pycache__
*.pyc

# Local virtual environment - container has its own isolated Python env
venv/

# Git metadata - not needed inside the container
.git/
.gitignore

# Local dev database - avoid baking a stale snapshot into the image
comments.db
